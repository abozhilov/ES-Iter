import './auto_mock_off';
import 'babel/polyfill';
import {cycle} from '../src/itertools';

describe('count', () => {
    it('indefinitely yields all of the elements of iterable', () => {
        
    })
})
